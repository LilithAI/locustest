# Restore Firm Intelligence (exact reverted build) + regen Locus Take\n\nScope is strict: only the directory's firm intelligence surface (`/directory` cards, `FirmDrawer`, `/directory/firms/:slug` profile page). Nothing else in the project changes — no global nav, no homepage, no other features touched.\n\n## 1. Schema (one migration)\n\n- Add `locus_take TEXT` to `firm_profiles` (nullable) if it does not already exist.\n- No other schema changes. Existing cleaned data (HQ fixes, normalized cities, nulled bad founded years, scrubbed addresses) stays as-is.\n\n## 2. Regenerate Locus Take for all 95 firms (against cleaned data)\n\nOne-shot script via Lovable AI Gateway (`google/gemini-2.5-flash`). Run from sandbox, write back via service role.\n\nPer-firm prompt input = ONLY fields from the cleaned `firm_profiles` row:\n- name, tier, firm_type, hq_city, offices[], office_count\n- founded_year (skip if null), total_lawyers, partner_count, partner_associate_ratio (skip if null)\n- practice_areas[], description (truncated)\n\nHard rules in the system prompt:\n- Output 2–3 sentences, max ~60 words, analyst tone (no marketing fluff).\n- Use ONLY the fields provided. If a field is null/missing, do not invent.\n- No claims about rankings, deals, clients, or news unless present in input.\n- No hedging (\