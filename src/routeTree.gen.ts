// Stub — replaced at build/dev time by the TanStack Router Vite plugin.
// This file exists only to satisfy TypeScript before the plugin runs.
import { createRootRoute } from "@tanstack/react-router";
export const routeTree = createRootRoute() as never;
